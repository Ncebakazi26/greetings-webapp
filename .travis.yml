language: node_js

env:
  - DATABASE_URL=postgresql://codex:pg123@localhost:5432/user

node_js:  
  - "12"

before_script:
  - psql -c 'create database  user;' -U postgres
  - psql -U postgres -d user -a -f table.sql

services:
  - postgresql